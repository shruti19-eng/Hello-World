name: Python CI

on: [push, pull_request]

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v2
    - name: Test welcome message
      run: python3 welcome.py
    - name: Test goodbye message
      run: python3 goodbye.py
